apply plugin: 'com.android.application'
apply plugin: "androidx.navigation.safeargs"

android {

    compileSdkVersion 29
    buildToolsVersion '29.0.3'
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        beta {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId "com.garethevans.church.opensongtablet"
        minSdkVersion 19
        targetSdkVersion 29
        multiDexEnabled true
    }

    dexOptions {
        jumboMode = true
        productFlavors {}
        testOptions {
            unitTests.returnDefaultValues = true
        }
    }

    buildscript {
        repositories {
            google()
            mavenCentral()
            jcenter()
        }
        dependencies {
            classpath 'com.android.tools.build:gradle:4.1.0'
            classpath "androidx.navigation:navigation-safe-args-gradle-plugin:2.3.1"
            //classpath 'me.tatarka:gradle-retrolambda:3.7.0'  //Only needed for Java 6/7
        }
    }

    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
        jcenter()
    }

    buildFeatures {
        viewBinding true
    }
    sourceSets {
        main {
            res {
                srcDirs 'src/main/res', 'src/main/res/layouts/TODELETE', 'src/main/res/layouts/TODELETE/layout'
            }
        }
    }
}

dependencies {
    implementation 'com.google.android.gms:play-services-cast:19.0.0'                   // For casting obviously!
    implementation 'com.google.android.gms:play-services-cast-framework:19.0.0'         // For casting obviously!
    implementation 'com.google.android.gms:play-services-nearby:17.0.0'                 // For Google Nearby Connect devices
    implementation 'com.google.android.gms:play-services-mlkit-text-recognition:16.1.1' // For OCR text recognition (PDF/Camera)
    implementation fileTree(include: ['*.jar'], dir: 'libs')                            // To allow local jar files in libs
    implementation 'androidx.annotation:annotation:1.1.0'                               // Allows annotations
    annotationProcessor 'androidx.annotation:annotation:1.1.0'                          // Allows annotations
    implementation 'androidx.appcompat:appcompat:1.2.0'                                 // Allows access to app: features for older devices
    implementation 'androidx.cardview:cardview:1.0.0'                                   // Cardview for recycler list items
    implementation 'androidx.constraintlayout:constraintlayout:2.0.2'                   // Constraint layouts with FAB
    implementation 'androidx.legacy:legacy-support-v4:1.0.0'                            // Older device support in views
    implementation 'androidx.lifecycle:lifecycle-extensions:2.2.0'
    implementation 'androidx.mediarouter:mediarouter:1.2.0'
    implementation 'androidx.navigation:navigation-fragment:2.3.1'
    implementation 'androidx.navigation:navigation-ui:2.3.1'
    implementation 'androidx.preference:preference:1.1.1'
    implementation 'androidx.recyclerview:recyclerview:1.1.0'
    implementation 'androidx.multidex:multidex:2.0.1'

    implementation 'com.google.android.material:material:1.2.1'

    implementation 'org.apache.commons:commons-compress:1.20'                           // For creating and editing compressed files (osb, backup)
    implementation 'com.github.bumptech.glide:glide:4.11.0'                             // For better setting of images
    annotationProcessor 'com.github.bumptech.glide:compiler:4.11.0'                     // For better setting of images
    //implementation 'com.fasterxml.jackson.core:jackson-core:2.9.8'
    implementation group: 'javax.xml.crypto', name: 'jsr105-api', version: '1.0.1'
    //implementation 'ru.egslava:MaskedEditText:1.0.5'                    // Not sure if I need this anymore
    implementation 'com.annimon:stream:1.2.1'                           // Not sure if I need this anymore
    implementation 'lib.kashif:folderpicker:2.4'                        // Not sure if I need this anymore
    //implementation 'com.github.mmin18:realtimeblurview:1.2.1'           // Not sure if I need this anymore
    implementation 'com.simplecityapps:recyclerview-fastscroll:2.0.1'   // Used for song and set menu
    implementation 'com.github.deano2390:MaterialShowcaseView:1.3.4'    // To highlight new features in the app
    implementation 'de.blox:graphview:0.7.0'                            // For file structure graph
    implementation 'com.otaliastudios:zoomlayout:1.8.0'                 // For file structure graph
}