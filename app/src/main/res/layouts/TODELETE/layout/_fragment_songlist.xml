<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/darkBackground"
    tools:context=".songsandsets.SongListFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include android:id="@+id/fragmentheader"
            layout="@layout/fragment_header" />

        <include android:id="@+id/songmenubuttons"
            layout="@layout/_song_menu_buttons" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:layout_margin="8dp">

            <include android:id="@+id/songmenualpha"
                layout="@layout/_song_menu_alphaindex" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical">
            <include layout="@layout/menu_songs_listtitles" />

                <com.simplecityapps.recyclerview_fastscroll.views.FastScrollRecyclerView
                    android:id="@+id/songListRecyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    app:fastScrollPopupBgColor="@color/mainSecondary"
                    app:fastScrollPopupTextColor="@color/white"
                    app:fastScrollThumbColor="@color/colorPrimary"
                    android:padding="4dp" />


            </LinearLayout>
        </LinearLayout>

    </LinearLayout>

    <ProgressBar
        android:id="@+id/wait_ProgressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addSongsButton"
        style="@style/Widget.Design.FloatingActionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_anchor="@+id/songListRecyclerView"
        android:layout_gravity="bottom|end"
        app:layout_anchorGravity="bottom|end"
        android:layout_margin="16dp"
        app:srcCompat="@drawable/ic_plus_white_36dp"
        app:layout_behavior="com.google.android.material.floatingactionbutton.FloatingActionButton$Behavior"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>