<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Dark Theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Font -->
        <item name="fontFamily">@font/lato</item>
        <item name="android:fontFamily">@font/lato</item>

        <!-- Text colors -->
        <item name="android:textColor">@color/dark_color</item>
        <item name="android:textColorPrimary">@color/dark_color</item>
        <item name="android:textColorSecondary">@color/dark_hint</item>
        <item name="android:textColorHighlight">@color/dark_secondary_variant</item>
        <item name="android:textColorHint">@color/dark_hint</item>
        <item name="hintTextColor">@color/dark_hint</item>
        <item name="color">@color/dark_color</item>"

        <!-- Primary Material Colors -->
        <item name="colorPrimary">@color/dark_primary</item>
        <item name="colorPrimaryFixed">@color/dark_primary</item>
        <item name="colorPrimaryVariant">@color/dark_primary_variant</item>
        <item name="colorOnPrimary">@color/dark_color</item>
        <item name="colorSecondary">@color/dark_secondary</item>
        <item name="colorSecondaryVariant">@color/dark_secondary_variant</item>
        <item name="colorOnSecondary">@color/dark_color</item>
        <item name="colorSurface">@color/dark_surface</item>
        <item name="colorSurfaceVariant">@color/dark_surface_variant</item>
        <item name="colorOnSurface">@color/dark_color</item>
        <item name="colorError">@color/dark_error</item>
        <item name="colorOnError">@color/white</item>
        <item name="errorTextColor">@color/dark_error</item>
        <item name="colorAccent">@color/dark_secondary_variant</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/dark_primary</item>
        <item name="colorBackgroundFloating">@color/dark_secondary</item>
        <item name="cardBackgroundColor">@color/dark_primary</item>
        <item name="backgroundColor">@color/dark_primary</item>

        <!-- Controls -->
        <item name="colorControlActivated">@color/dark_secondary</item>
        <item name="colorControlNormal">@color/dark_color</item>
        <item name="colorControlHighlight">@color/dark_secondary</item>
        <item name="colorButtonNormal">@color/dark_secondary</item>

        <!-- Button & FAB overrides -->
        <item name="materialIconButtonFilledStyle">@style/FilledButton</item>
        <item name="materialButtonStyle">@style/FilledButton</item>
        <item name="materialButtonOutlinedStyle">@style/OutlinedButton</item>
        <item name="floatingActionButtonStyle">@style/FABStyle</item>
        <item name="extendedFloatingActionButtonStyle">@style/ExtendedFABStyle</item>

        <!-- MyMaterialTextView -->
        <item name="myMaterialTextViewStyle">@style/Widget.MyApp.MyMaterialTextView</item>

        <!-- Bottom Sheets -->
        <item name="bottomSheetDialogTheme">@style/BottomSheetDialogTheme</item>

        <!-- Switches -->
        <item name="materialSwitchStyle">@style/MySwitchStyle</item>
        <item name="switchStyle">@style/MySwitchStyle</item>

        <!-- Checkboxes -->
        <item name="checkboxStyle">@style/MyCheckboxStyle</item>

        <!-- Tuner -->
        <item name="tunerColor">@color/dark_secondary</item>
        <item name="tunerOff">@color/dark_tuner_off</item>
        <item name="tunerGreen">@color/dark_tuner_green</item>
        <item name="tunerRed">@color/dark_tuner_red</item>

        <!-- Animations -->
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>

        <!-- Menu items -->
        <item name="actionButtonStyle">@style/ActionButtonStyle</item>

        <item name="popupMenuBackground">@drawable/exposed_popup</item>

        <!-- Default dialog (for Nearby) -->
        <item name="alertDialogTheme">@style/MyAlertDialogTheme</item>
    </style>
</resources>