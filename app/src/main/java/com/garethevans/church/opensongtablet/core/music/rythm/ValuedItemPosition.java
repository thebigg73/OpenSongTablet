package com.garethevans.church.opensongtablet.core.music.rythm;

public enum ValuedItemPosition {

    /** On top of the {@link com.garethevans.church.opensongtablet.core.music.stave.Stave}. */
    TOP,

    /** At the bottom of the {@link com.garethevans.church.opensongtablet.core.music.stave.Stave}. */
    BOTTOM,

    /** Above the {@link ValuedItem item}. */
    ABOVE,

    /** Below the {@link ValuedItem item}. */
    BELOW,

    /** Left to the {@link ValuedItem item}. */
    LEFT,

    /** Rigth to the {@link ValuedItem item}. */
    RIGHT
}
