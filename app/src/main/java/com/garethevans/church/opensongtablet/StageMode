// To level imports

public String convertToWebContent(String mystring) {

  // This is one section of the song
  // Split the text into separate lines

  String[] mylines = mystring.split("/n");
  String myHTML = "";

  // Go through section and identify line as lyric chord or comment
  String[] linetype = new String(mylines.length());
  for (int x=0; x<mylines.length; x++) {
    if (mylines[x]!=null && mylines[x].startsWith(".")) {
      linetype[x] = "chord";
    } else if (mylines[x]!=null && mylines[x].startsWith(";")) {
      linetype[x] = "comment";
    } else {
      linetype[x] = "lyric";
    }

  // Now we know what the lines are, format them properly
  // Firstly if this is a chord line and the line after is
}
