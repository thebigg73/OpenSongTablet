package com.garethevans.church.opensongtablet.drummer;

import android.view.View;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;

public class DrumViewGridHolder extends RecyclerView.ViewHolder {

    // A simple view holder (an empty view) used for the drum editor grid
    public DrumViewGridHolder(@NonNull View drumViewGridHolder) {
        super(drumViewGridHolder);
    }

}
